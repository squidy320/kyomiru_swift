import SwiftUI

enum Theme {
    static let baseBackground = Color.black
    static let accent = Color(red: 0.47, green: 0.72, blue: 1.0)
    static let textPrimary = Color.white
    static let textSecondary = Color(red: 0.63, green: 0.66, blue: 0.74)
}
